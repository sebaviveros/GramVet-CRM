# Documento de traspaso — GramVet CRM (2026-06-28)

> Para una instancia nueva de Claude Code que NO vio la conversación anterior. Leé esto completo y podés retomar sin pedirle nada al usuario. Hay además memoria persistente del proyecto en `C:\Users\svive\.claude\projects\E--Workspace-GRAMVET-GramVet-CRM\memory\` (se carga sola cada sesión: `MEMORY.md`, `project_context.md`, `user_profile.md`, `feedback_workflow.md`) — este documento la complementa y prioriza lo de la última sesión.

---

## 0. PUNTO EXACTO DONDE QUEDAMOS

Se acordó arrancar la **Fase 1 de la automatización IA** (extracción + formulario de confirmación). **Todavía NO se escribió nada de código de esta feature.** Quedó definido: modelo `claude-haiku-4-5`, integración por HttpClient, structured outputs, con **fallback simulado** porque la API key aún no está disponible (ver sección 1). El próximo paso concreto es implementar la Fase 1 (diseño detallado en sección 1, "Diseño concreto de Fase 1"). Antes, verificar `git status` y qué migraciones de BD corrió el usuario (sección 5).

---

## 1. OBJETIVO ACTUAL (lo que estábamos por empezar)

**Desarrollar la "automatización": botón "Agendar cita (IA)" en el chat → extraer datos de la conversación con Claude → crear evento en Google Calendar de la veterinaria.**

Es **el corazón del proyecto**: el cliente (dueño de la veterinaria) se fue de respond.io/n8n porque quería automatizar el agendado. El inbox multicanal ya está construido (paridad con respond.io); lo que justifica el CRM propio es esta automatización + ser dueño de los datos + no pagar USD 300/mes.

### Decisiones ya tomadas para esta feature (NO re-discutir)
- **Modelo:** `claude-haiku-4-5` (decisión del usuario por costo, ~9 CLP/cita típica; suficiente para extracción estructurada). Debe ser **configurable** en `appsettings.json`.
- **API key de Anthropic: el usuario AÚN NO la tiene.** Le pasaron recién una cuenta de Claude con **USD 20 en créditos**. Debe generar la API key en `console.anthropic.com` y pegarla en `appsettings.json` (sección `Anthropic`). Con Haiku 4.5 (~US$0.009/cita) esos 20 USD alcanzan para ~2000+ extracciones — sobra para dev y pruebas. **Mientras no tenga la key, desarrollar todo con el fallback simulado** (devuelve borrador con datos del contacto, marca `[IA SIMULADA]`).
- **Provider/integración:** llamar la Claude API por **HttpClient** (mismo patrón que WhatsApp/Meta/R2). **NO** agregar NuGet `Anthropic`. Endpoint `POST https://api.anthropic.com/v1/messages`, headers `x-api-key`, `anthropic-version: 2023-06-01`, `content-type: application/json`. Usar **structured outputs** (`output_config.format` con JSON schema; soportado en Haiku 4.5) para que devuelva exactamente los campos del molde. `max_tokens` ~1024-2048. No mandar `thinking` (Haiku no usa adaptive/effort).
- **Fase 1 (lo que sigue, confirmado):** extracción con IA + **formulario de confirmación editable**. SIN crear el evento todavía.
- **Fase 2 (después):** crear el evento en Google Calendar — **bloqueada** hasta que (a) el dueño explique cómo organiza su calendario y (b) se defina auth de Google (OAuth / service account).
- Antes de codear la IA ya se consultó la skill `claude-api` (referencia oficial). Si necesitás detalles de la API, volvé a invocarla; los esenciales están acá.

### Formato EXACTO del evento de calendario (sacado de capturas reales del cliente) — es el SCHEMA de salida de la IA
- **Título:** `[rango horario] [comuna/sector]: [dirección] +[teléfono] [nombre cliente]`
  - Ej: `1 - 3 viña (reñaca alto): Lago villarrica 599, Block C depto 42 +56959280537 Ailin F...`
- **Descripción** (secciones fijas, en este orden):
  - `Paciente(s):` (ej. "1 gato")
  - `Cliente solicitó:`
  - `Se cobró:` (líneas, ej. "$19.000 triple / $6.000 ida / $6.000 inyectables c/u")
  - `Total mínimo a cobrar:` (ej. "$25.000")
  - `Referencias para encontrar el domicilio:`
  - `Observaciones:`
  - `Correo:`
- **Sobre los slots vacíos pre-creados del dueño:** crea citas vacías recurrentes (bloques solapados de 2h) como plantilla. Decisión (Claude): NO rellenar esos huecos (matching frágil); **crear eventos NUEVOS** en la fecha/hora confirmada. El dueño está OK con cambiar su sistema. Lo único intocable es el formato título+descripción.
- La feature también debe **autocompletar datos del cliente** (dirección, etc.) en el panel de contacto al usar la IA (punto del TODO).
- El botón de iniciar el flujo IA va **debajo del nombre del cliente** en el panel de contacto (punto del TODO).

### Diseño concreto de Fase 1 a implementar (no empezado — no se escribió código aún)
- `appsettings.json` (lo maneja el usuario, NO commitear): agregar sección `"Anthropic": { "ApiKey": "<el usuario la pega>", "Model": "claude-haiku-4-5" }`.
- **Backend** (`GramVetCRM.Service`): nuevo `IAgendaIaService` / `AgendaIaService` con `IHttpClientFactory` + `IConfiguration` + repos (mensaje, contacto, conversación). Método `ExtraerCita(int conversacionId)`:
  1. Cargar la conversación + últimos N mensajes de texto + datos del contacto (nombre, teléfono, dirección, referencias, mascotas).
  2. Construir prompt (system + user con el transcript) y llamar la Messages API con `output_config.format` (JSON schema con los campos del molde).
  3. Parsear el JSON → DTO; armar `tituloEvento` + `descripcionEvento` en el formato exacto de arriba.
  4. **Fallback** si `ApiKey` vacío: devolver borrador con datos del contacto + marca `[IA SIMULADA]` (igual patrón que `EmailService`).
- DTO `CitaExtraidaDto`: pacientes, clienteSolicito, cobros, totalMinimo, referenciasDireccion, observaciones, correo, nombreCliente, telefono, direccion, comunaSector, fechaHoraSugerida, tituloEvento, descripcionEvento.
- Registrar el service Scoped en `Program.cs` (+ `AddHttpClient` si hace falta).
- Endpoint: `POST /api/Conversacion/{id}/extraer-cita` (o un `CitaController` nuevo). Devuelve el DTO.
- **Frontend:** método `extraerCita(id)` en `conversacion.service.ts`; botón "Agendar cita (IA)" en `contact-panel.component` debajo del nombre; al hacer click → loading → mostrar formulario de confirmación editable (Swal o panel) con lo que extrajo la IA. (Crear evento = Fase 2; por ahora solo mostrar/confirmar.)
- **A definir con el usuario:** si el botón lo ven todos los roles o solo staff (el veterinario no puede editar contacto, pero sí podría agendar — preguntar). Y si conviene gatear el endpoint por costo.

---

## 2. CÓMO TRABAJA EL USUARIO (preferencias — respetar siempre)

- **Sebastian Viveros** — project lead, arquitectura e implementación. Trabaja con un frontend dev separado. Primera vez usando Claude Code (antes trabajaba por chat con archivos completos). Escribe en español (a veces argentino/chileno).
- **Nunca modificar código sin autorización explícita.** Proponer primero, esperar el "sí", recién entonces Edit/Write/Bash. (En la práctica de esta sesión, cuando dice "sigamos" / "dale" / "desarrollemos X" eso ES la autorización para esa tarea.)
- **Leer el archivo completo antes de editarlo** — nunca asumir contenido.
- Trabaja iterativo: prueba cada feature antes de seguir.
- **Al final de cada sesión pide un mensaje de commit** (conventional commits: `feat: <resumen>` + cuerpo por áreas, en español). Termina los commits con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Prefiere reportes concretos de qué se hizo y por qué, no solo el código.
- Separación de responsabilidades: el CRUD (crear/editar/eliminar) vive en el módulo de gestión; el punto de uso (chat/panel) solo selecciona/asigna lo ya creado.

---

## 3. STACK Y DATOS NO RECONSTRUIBLES

- **Frontend:** Angular 21, CoreUI Angular, standalone components, Angular Signals, SCSS. Build de verificación: `cd Frontend && npx ng build --configuration development`. Fuentes globales DM Sans/DM Mono ya importadas en `src/scss/_custom.scss`.
- **Backend:** ASP.NET Core .NET 8, C#, EF Core, SQL Server. Capas: `Api (Controller) → Service → Repository → Model`. Interfaces para Repository y Service, registradas Scoped en `Program.cs`. EF mapea por convención (SQL Server case-insensitive: `Usercr`↔`usercr`).
- **BD:** `GramVetCRM_DEV` en `localhost\SQLEXPRESS`, `Trusted_Connection=True;TrustServerCertificate=True`. Hay `sqlcmd` disponible en `C:\Program Files\Microsoft SQL Server\Client SDK\ODBC\170\Tools\Binn\sqlcmd`.
- **Real-time:** SignalR vía `ChatHub` (`GramVetCRM.Service/Hubs`). JWT por query string (`?access_token=`). **SignalR serializa a camelCase**: props PascalCase del DTO/objeto anónimo → camelCase en el front. Grupos: veterinario → `user-{id}`; admin/secretario → `staff`. Emisiones van a staff + user-{asignado}.
- **Media:** Cloudflare R2 (S3-compatible), bucket `gramvetcrm-media`, URL pública `https://pub-fa94e83d8318492d87b9d41e72b93de0.r2.dev`. `R2StorageService` (Singleton) con `SubirArchivo(stream, fileName, contentType)` → URL pública, y `EliminarArchivo(key)`. La key se deriva de la URL con `new Uri(url).AbsolutePath.TrimStart('/')`.
- **URLs dev:** Backend `https://localhost:7101`, Frontend `http://localhost:4200`.
- **ngrok:** carpeta `E:\Workspace\Ngrok\ngrok-v3-stable-windows-amd64`. Comando: `.\ngrok.exe http https://localhost:7101 --host-header="localhost:7101"`. ⚠️ ngrok free cambia la URL en cada reinicio → hay que actualizar el callback en los webhooks de Meta (WhatsApp `/api/WhatsApp/webhook` y Meta `/api/Meta/webhook`).
- **WhatsApp (Cloud API):** número de prueba **+56 9 2172 4181**, PhoneNumberId `1158710177318339`. ⚠️ **RESTRICCIÓN CRÍTICA: nunca tocar el WhatsApp Business real del cliente ni su configuración productiva. Probar siempre con el número de prueba.** Verify token webhook: `gramvet_gonzalord`.
- **Meta (Messenger/IG):** app **VetCRM** (ID en URL `1679170393092984`). Página FB "GramVet Médico Veterinario a Domicilio V Región", **Page ID `101078882762015`**. Instagram **@gramvet.cl** vinculado a la página (confirmado en esta sesión). Verify token Meta: `gramvet_meta_verify`. Webhook Meta endpoint `/api/Meta/webhook` (compartido Messenger+IG; el backend distingue por `body.object`).
- **appsettings.json:** TIENE credenciales sensibles (WhatsApp token, Meta PageAccessToken, R2 keys, JWT, Gmail App Password, y ahora la Anthropic API key) y **está trackeado en git**. **Sebastian se encarga manualmente de NO subirlo.** NUNCA commitearlo.
- **Roles (tabla Rol):** 1=Admin, 2=Secretario, 3=Veterinario. Detección admin front: `rolNombre.toLowerCase().includes('admin')`. `esStaff` = admin || secretario. En `AuthService` (front) hay `isAdmin()`, `isVeterinario()`, `isStaff()`.
- **Patrones:** soft delete (`Active=false` + `Userup`/`Fechaup`, nunca borrar filas). Columnas auditoría: `usercr`, `userup`, `fechacr`, `fechaup`, `active`.
- **Email:** `EmailService` (System.Net.Mail) funcionando, remitente `gramvetadministracion@gmail.com` (Gmail App Password, 2FA). Fallback a consola si no hay credenciales.

### ⚠️ Gotcha de build del backend (IMPORTANTE)
Cuando la API está corriendo (proceso `GramVetCRM.Api`) y/o Visual Studio abierto, `dotnet build` de la solución falla con **MSB3021/MSB3027 (no puede copiar el DLL, archivo bloqueado)** — **NO son errores de código**. Para verificar que el código compila sin parar la app, compilar un proyecto suelto: `dotnet build GramVetCRM.Service/GramVetCRM.Service.csproj -nologo -clp:ErrorsOnly` (0 errores = OK). Para reconstruir de verdad, el usuario para la app desde Visual Studio.

---

## 4. ESTADO ACTUAL — RESUELTO en esta sesión (todo compila; front buildea)

### Features con BD (end-to-end backend + frontend) — HECHAS
- **Dirección + referencias del cliente:** `Contacto.Direccion` + `ReferenciaDireccion`. Editable en contact-panel.
- **Bitácora de mascota:** tabla `MascotaBitacora` (1 fila = 1 anotación). Endpoints en `MascotaController` (`{id}/bitacora`, `bitacora` POST, `bitacora/{id}` DELETE). El service resuelve el nombre del autor desde `usercr`. UI: botón libro por mascota → panel con textarea + lista fechada.
- **Fotos de mascota:** tabla `MascotaFoto` (URL R2). `MascotaFotoService` sube a R2 y al borrar elimina el objeto. UI: botón imagen → grid de miniaturas + lightbox.
- **Visibilidad de etiquetas/respuestas por veterinario:** tablas `EtiquetaUsuario` / `RespuestaRapidaUsuario`. **Estricto: admin/secretario ven todo; veterinario ve solo lo vinculado** (sin vínculo = no la ve). `GetAll(int? veterinarioId)` filtra por rol (claim `rolNombre`). DTOs llevan `VeterinarioIds`. Gestión (tags/macros) con multiselect de checkboxes de veterinarios.

### Mensajería
- **Geolocalización (enviar/recibir):** `Mensaje.TipoMensaje="location"`, `MediaUrl`=URL Google Maps (`?q=lat,lng`), `Contenido`=nombre/dirección. Helpers `WhatsAppService.MapsUrl` + `ResumenMensaje`. Envío: botón en el footer usa `navigator.geolocation` → WhatsApp nativo; Messenger/IG como link de Maps. ⚠️ Geolocation requiere HTTPS o localhost; en desktop la precisión es baja por IP (no es bug; en celular usa GPS). ⚠️ Messenger deprecó recibir ubicación (~2019); IG no la soporta — WhatsApp es el canal real.
- **Emojis:** botón 😊 en el composer abre panel; insertan en el input. NO se cierra al elegir (como WhatsApp), se cierra con **click afuera** (`@HostListener('document:click')` + `stopPropagation` en botón y panel).
- **Reacciones (solo WhatsApp):** columna `Mensaje.Reaccion`. Recibir: webhook `type:"reaction"` → `WhatsAppService.ProcesarReaccion` ubica por `ExternalId` y guarda emoji; SignalR `MensajeReaccionado`. Enviar: hover sobre mensaje → picker (👍❤️😂😮😢🙏) → `POST /api/Conversacion/mensaje/{id}/reaccion` → `WhatsAppService.EnviarReaccion`. Messenger/IG lanzan error (no soportado). Se capturó el `ExternalId` saliente: los métodos de envío de WhatsApp y `MetaMessagingService.EnviarMensaje` ahora devuelven el wamid/message_id (antes bool); `ConversacionService.EnviarMensaje` lo guarda en `Mensaje.ExternalId`.
- **"Visto" (estado de entrega, solo WhatsApp):** columna `Mensaje.EstadoEntrega` (sent/delivered/read/failed). Webhook `value.statuses` → `WhatsAppService.ProcesarEstados` (solo avanza el estado, nunca retrocede) → SignalR `MensajeEstado`. `EnviarMensaje` setea "sent" inicial (solo WhatsApp). Front: `tickSymbol()` + render ✓/✓✓/✓✓ azul en texto/imagen/ubicación salientes. **Decisión: solo "ver si el cliente leyó mis mensajes"** (NO marcar como leídos los del cliente).
- **Stickers:** WhatsApp recibe/renderiza stickers (descarga `.webp`, render SIN burbuja con clase `.sticker-msg`, ~120px; resumen "Sticker").
- **Emojis dentro de texto** ya funcionaban en los 3 canales (Unicode) — no requirió nada.

### Permisos y bugs
- **Veterinarios solo acceden al Buzón:** nav filtrado en `default-layout.component.ts` + guards de ruta. Guard `staff.guard.ts` (admin/secretario) en `tags`/`macros`; `users` ya tenía `adminGuard`. Veterinario por URL directa → redirige a `/inbox`.
- **Permisos del veterinario en contact-panel:** NO ve "Editar contacto" ni la ✕ de quitar etiquetas (`puedeEditarContacto`/`puedeQuitarEtiqueta` = !veterinario). SÍ puede asignar etiquetas (solo las habilitadas), y SÍ mascotas/bitácora/fotos.
- **Bug de auditoría de usuarios (root cause real):** el modelo C# `Usuario` NO tenía `Usercr/Userup/Fechacr/Fechaup`, por eso EF nunca escribía `usercr` (quedaba NULL). Se agregaron al modelo + `UsuarioService` ahora registra al admin actor en crear/editar/eliminar/reset/cambiar pass (firmas cambiadas para recibir `actorUsuarioId`; controller lo pasa desde el JWT). Usuarios viejos (filas 2-6) quedaron con usercr NULL (NO se backfilleó; el usuario dijo que no era necesario).
- **Foto de perfil de usuarios:** columna `Usuario.FotoUrl`. `POST /api/Usuario/foto` (sube a R2, borra anterior) y `GET /api/Usuario/me`. Front: header (menú "Mi cuenta" → "Cambiar foto de perfil") muestra la foto; lista de Usuarios muestra avatar con foto. Cada usuario edita la SUYA. `UsuarioService` ahora inyecta `IR2StorageService`.

### Quick wins / UI
- Quitado el **filtro por estado** del buzón (el cliente usa etiquetas; `Conversacion.Estado` sigue en DTO/backend por si se reactiva).
- "Filtros:" en el header del buzón; rótulos (Teléfono:/Email:/Canal:) en contact-panel; ícono de adjuntos SVG empresarial (en vez de 📎).
- **Modal de confirmación al asignar/desasignar veterinario** (revierte el `<select>` si se cancela).
- **Indicador de veterinario asignado:** se hizo primero como badge flotante en el chat, pero el usuario lo pidió **movido a la lista de conversaciones** (chip con iniciales + nombre a la derecha de cada conversación). El badge del chat se ELIMINÓ.
- **Bug lightbox de fotos arreglado:** la animación `.panel-opening` con `forwards` dejaba un `transform` permanente en la columna del panel → atrapaba el `position:fixed` del lightbox. Fix: quitar `forwards` en `inbox.component.scss`.
- **Bug conversación seleccionada + no leídos:** `upsertConversacion` fuerza no leídos=0 si es la conversación abierta; SignalR marca leído al llegar mensaje a la activa; CSS `.conversation-item.unread:not(.selected)`.
- **Selector de rol en edición de usuario:** usaba `[value]` (no marcaba bien con `@for`); fix con `[selected]="rol.id === editRolId()"` en la `<option>`.
- **Header del chat clickeable:** alterna el panel de contacto (antes solo lo abría). Hover con tinte **violeta `#321fdb`** (el `$secondary` de `styles.scss`, mismo de los botones del composer) — el usuario lo pidió así. Cursor pointer + tooltip.
- **Panel de contacto oculto si no hay conversación seleccionada** (`state.isRightPanelOpen() && state.selectedConversation()`).
- **Mensajes largos en la lista** se recortan a una línea con "…" (`.conversation-content { flex:1; min-width:0 }` + ellipsis en name/phone/last-message).
- **Preview del último mensaje con íconos:** `previewUltimoMensaje()` mapea `[image]`→📷 Imagen, `[audio]`→🎵 Audio, etc. (maneja datos viejos y nuevos).
- **Logo del sidebar** reemplazado por el oficial (`assets/logos/logoNavBarGVCRM.svg`, versión "(3)") y **imagen del login** (`assets/images/loginGramVet.png`, ruta actualizada en `login.component.scss`). Imágenes viejas/no usadas borradas.
- **Estandarización de estilos:** módulos `tags` (etiquetas) y `macros` (respuestas rápidas) alineados al estándar del módulo **Usuarios**: header padding 18/24, ícono 36px radius 10 bg `#1A4A3C` stroke `#6ECFAB` 18px, título 15px, subtítulo 12px `#6c757d`, badge pill claro (`#E1F5EE`/`#085041`) DM Mono, section-label 11px, host DM Sans.

### Removido al inicio de la sesión
- **Gestión manual de estado de conversación: DESCARTADA** por el cliente (usa etiquetas). Filtro por estado removido de la UI.

---

## 5. ESTADO DE LA BD — MIGRACIONES

Scripts en `Database/Scripts/`. Se actualizaron SIEMPRE dos cosas: el **canónico** (instalar prod de cero) y una **migración** (BD dev ya creada).

**Canónicos actualizados:** `01` (Usuario.FotoUrl), `03` (Contacto dirección/referencia), `06` (Mensaje.Reaccion + EstadoEntrega), `13_create_table_MascotaFoto.sql`, `14_create_table_MascotaBitacora.sql`, `15_create_table_EtiquetaUsuario.sql`, `16_create_table_RespuestaRapidaUsuario.sql`.

**Migraciones en `Database/Scripts/Migrations/` (idempotentes):**
- `2026-06-23_features_mascota_contacto_visibilidad.sql` (Contacto dir/ref + MascotaFoto + MascotaBitacora + EtiquetaUsuario + RespuestaRapidaUsuario) — **el usuario YA la corrió.**
- `2026-06-23_mensaje_reaccion.sql` (Mensaje.Reaccion) — ⚠️ verificar si la corrió.
- `2026-06-25_usuario_foto.sql` (Usuario.FotoUrl) — ⚠️ verificar si la corrió.
- `2026-06-25_mensaje_estado_entrega.sql` (Mensaje.EstadoEntrega) — ⚠️ verificar si la corrió.

> Al retomar: confirmar con el usuario qué migraciones ya aplicó en `GramVetCRM_DEV`. Si una columna falta, el backend tira error al usar esa feature.

---

## 6. ABIERTO / PENDIENTE (en orden recomendado)

1. **Feature IA + Google Calendar — Fase 1** (lo que sigue YA): extracción + formulario de confirmación. Detalle completo en la sección 1. El usuario tiene la API key y eligió Haiku 4.5.
2. **Feature IA + Google Calendar — Fase 2:** crear el evento en Google Calendar. Bloqueada por (a) el dueño debe explicar cómo organiza su calendario, (b) definir auth de Google (OAuth/service account).
3. **Integración Instagram:** el backend YA lo soporta (`MetaController` + `MetaMessagingService`, object "instagram", prefijo `IG:`, envío por `/me/messages`). PERO Meta exige **app publicada + App Review** para recibir webhooks de IG (la vía nueva "Instagram API" lo pide explícitamente: "Para recibir webhooks, la app debe estar publicada"). respond.io funciona porque su app ya pasó App Review. **Conclusión: Instagram se cierra en el hito de App Review / Go Live, no antes.** No es problema de código.
4. **"Visto" en Messenger/IG:** pendiente. El usuario quiere que funcione también si las apps reales lo permiten. Messenger SÍ se puede (eventos `message_deliveries`/`message_reads` por webhook, hay que suscribirlos en Meta; el "leído" es por **watermark**/timestamp, no por message id como WhatsApp → código aparte). Instagram: limitado/poco confiable. Conviene hacerlo cuando se toque la config de webhooks de Meta (junto con Instagram).
5. **Reacciones en Messenger/IG:** recibir requiere suscribir `message_reactions`; enviar no lo permite la API de páginas. Pendiente (nice-to-have).

### Deuda técnica (antes de producción)
- **Validación de rol en el BACKEND** para endpoints de gestión (Usuarios, Etiquetas, Respuestas). Hoy el control es **solo frontend** (nav + guards). Un veterinario técnico podría llamar la API directo. Patrón a replicar: `ConversacionController` endpoint asignar (`if (!rolLower.Contains("admin") && !rolLower.Contains("secretario")) return Forbid();`). Recomendado, no urgente. El usuario dijo "dejalo así por ahora".
- Mover secretos de `appsettings.json` fuera de git (`appsettings.Development.json` + `.gitignore`).
- Limpiar rutas demo del template (charts, base, buttons, etc.) accesibles por URL pero no en el menú.

### Para producción
- App de Meta a **Live + App Review** (verificación del negocio + permisos `pages_messaging`, `instagram_manage_messages`, etc. + video demo). Necesario para WhatsApp/Messenger/Instagram con clientes reales. Es un hito con burocracia de Meta (días/semanas).
- Hosting + HTTPS real (la geolocalización necesita HTTPS; ngrok cambia de URL al reiniciar). Considerar dominio estático de ngrok mientras tanto.

### Fuera de alcance (NO desarrollar)
- **Dashboard** — explícitamente sacado del scope por Sebastian.

---

## 7. GIT / COMMITS

- Los grandes bloques se fueron commiteando durante la sesión (Sebastian pidió mensajes de commit varias veces y commiteó). El último commit conocido al inicio era `3c468b3 SVN: nuevo logo y login, y ajuste de permisos del veterinario`.
- ⚠️ **Al retomar: correr `git status`** — es probable que haya cambios sin commitear de la última tanda (foto de perfil de usuarios, estandarización de estilos, bugs de stickers/emoji-click-afuera/conversación-seleccionada, y cualquier ajuste posterior). La feature de IA todavía NO tiene código escrito.
- Recordatorio al commitear: **no subir `appsettings.json`**; conventional commits en español; cerrar con `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

---

## 8. CÓMO RETOMAR (primeros pasos sugeridos)

1. Leé la memoria del proyecto (se carga sola) + este documento.
2. `git status` para ver qué quedó sin commitear.
3. Confirmá con Sebastian qué migraciones de BD corrió (sección 5).
4. Arrancá la **Fase 1 de la IA** (sección 1, "Diseño concreto"): backend extraction service + endpoint + frontend botón + formulario. Modelo `claude-haiku-4-5` configurable. **La API key aún no está** (el usuario tiene cuenta de Claude con USD 20 en créditos, debe generar la key) → **construí con el fallback simulado**; cuando el usuario pegue la key en `appsettings.json` (sección `Anthropic`) funciona la IA real sin más cambios. Antes de tocar archivos, recordá: proponer y esperar OK no es estrictamente necesario tarea por tarea (ya autorizó la feature), pero leé cada archivo completo antes de editarlo y verificá build (Service suelto si la API está corriendo; `ng build` para el front).
