# Documento de traspaso — GramVet CRM (2026-07-09)

> Para una instancia nueva de Claude Code que NO vio esta conversación. Leé esto completo y podés retomar sin pedirle nada al usuario. Hay memoria persistente del proyecto en `C:\Users\svive\.claude\projects\E--Workspace-GRAMVET-GramVet-CRM\memory\` (se carga sola: `MEMORY.md`, `project_context.md`, `user_profile.md`, `feedback_workflow.md`, `calendar_logic.md`). Este doc prioriza lo de esta sesión. El traspaso anterior es `Docs/TRASPASO_2026-06-28.md` (contexto base del proyecto, stack, Meta/R2, etc. — sigue vigente salvo lo que este actualiza).

---

## 0. PUNTO EXACTO DONDE QUEDAMOS

La **feature de IA + Google Calendar está terminada y funcionando de punta a punta con datos reales** (extracción con Claude Haiku, creación de eventos en Google Calendar de prueba, creación de mascotas, autocompletado de perfil). La tanda visual del buzón ya está pusheada (`13435d8`).

**Sesión del 2026-07-09 (parte 2) — SIN COMMITEAR.** Tres bloques (detalle en la sección 2.J):
1. **Rediseño gris + verde del buzón** (los 3 paneles en `#2B2B2B`, verdes como acento), separadores de fecha estilo WhatsApp en el chat, foto del vet en el chip de asignado, burbuja morada con nombre/rol en el header.
2. **Captcha Cloudflare Turnstile en el login** (backend + frontend, funcionando).
3. **Environments centralizados** (`src/environments/`) + **sesión persistente para los veterinarios** (localStorage, interceptor de 401, token a 10 h).

**Lo inmediato pendiente:** commitear/pushear (ver sección 4, NO subir `appsettings.json`) y **rotar la API key de Anthropic** si no se hizo (quedó expuesta en un chat anterior, ver sección 3).

---

## 1. CÓMO TRABAJA EL USUARIO (respetar SIEMPRE — aprendido esta sesión)

- **Sebastian Viveros** — project lead. Escribe en español (chileno/argentino). Trabaja iterativo: prueba cada cosa antes de seguir.
- **Sebastian hace los commits y push él mismo. Claude SOLO entrega título + descripción.** NUNCA ejecutar `git add/commit/push`. El título estilo `SVN: <resumen>`; la descripción **resumida y poco técnica** (qué cambió en términos de producto/usuario, no implementación). ⚠️ **NO agregar el trailer `Co-Authored-By`** (Sebastian lo pidió quitar el 2026-07-09); el mensaje termina en la descripción, sin firma.
- **Sebastian aplica él mismo los scripts de BD / migraciones** (sqlcmd contra `GramVetCRM_DEV`). Claude SOLO entrega el `.sql` o el comando listo; no ejecutar sqlcmd.
- **No pegar el `appsettings.json` completo ni credenciales** — si hay que verificar config, pedir/mostrar solo la sección puntual.
- Leer cada archivo completo antes de editarlo.
- Al pushear, **NUNCA subir `appsettings.json`** (tiene secretos y está trackeado; Sebastian lo excluye a mano).

---

## 2. LO QUE SE CONSTRUYÓ EN ESTA SESIÓN

### 2.A — Feature IA: "Agendar cita (IA)" (Fase 1 + Fase 2) — COMPLETA Y FUNCIONANDO

Botón en el panel de contacto del buzón (solo **admin/secretario**) → extrae datos del chat con Claude → formulario editable → crea el evento en Google Calendar + registra mascotas + completa perfil del cliente.

**Backend (todo nuevo):**
- `Model/DTOs/Cita/CitaExtraidaDto.cs` — resultado de extracción (nombre, teléfono, dirección, comunaSector, referencias, correo, pacientes, clienteSolicito, cobros, totalMinimo, observaciones, fechaHoraSugerida, ubicacionGps, seguroMascota, estacionamientoVisita, **mascotas** (lista `MascotaCitaDto` nombre+especie), slotSugerido, tituloEvento, descripcionEvento, simulada).
- `Model/DTOs/Cita/CrearCitaDto.cs` — request de confirmación (fecha, movil, slotIndex, tituloEvento, descripcionEvento, ubicacion, mascotas, + nombreCliente/direccion/referenciasDireccion/correo para autocompletar perfil) y `CitaCreadaDto` (eventoId, eventoLink, inicio, fin, mascotasCreadas, camposPerfilActualizados, simulada).
- `Service/AgendaIaService/` — `IAgendaIaService`/`AgendaIaService`:
  - `ExtraerCita(conversacionId)`: carga conversación + contacto + mascotas + últimos 40 mensajes de texto → llama `POST https://api.anthropic.com/v1/messages` por **HttpClient** ("Anthropic", registrado en Program.cs) con **structured outputs** (`output_config.format`, JSON schema, todos los campos `required` + `additionalProperties:false`, mascotas como array de objetos). Modelo `claude-haiku-4-5` (config `Anthropic:Model`). **Fallback simulado** si `Anthropic:ApiKey` vacío o ante cualquier excepción (marca `[IA SIMULADA]`; NO inventa mascotas). SIN `thinking`/`effort` (Haiku no los soporta).
  - `CrearCita(conversacionId, dto, actorId)`: (1) calcula posición con `AgendaSlots.CalcularPosicion`; (2) crea SOLO mascotas nuevas (dedup exacto por nombre contra las existentes + dedup dentro del request); (3) **completa campos VACÍOS del perfil** del contacto (dirección, referencia, email; y nombre si el contacto entró sin nombre o como su teléfono) — nunca pisa datos existentes; (4) crea el evento vía `IGoogleCalendarService`.
  - `AgendaSlots.cs` (static) — catálogo de horarios + algoritmo de posicionamiento. **Ver la lógica exacta en la memoria `calendar_logic.md` (crítica, confirmada por el cliente).** Resumen: 10 slots fijos; hora real va en el TÍTULO; el evento se posiciona en un bloque de 1 hora codificado según móvil (Móvil 1 = franja AM desde 01:00 + i horas; Móvil 2 = franja PM desde 13:00 + i horas). Validación: slot i=9 (segundo "6-7:30") solo Móvil 1 y solo miércoles a sábado.
- `Service/GoogleCalendarService/` — `IGoogleCalendarService`/`GoogleCalendarService`: usa el NuGet **`Google.Apis.Calendar.v3` (v1.68.0.3680)** con **Service Account** (`GoogleCredential.FromStream(...).CreateScoped(Calendar)`). Crea el evento (`Events.Insert`), start/end con `DateTimeOffset` + `TimeZoneInfo.Local` + `TimeZone` de config. **Fallback simulado** si falta la config o el archivo de clave. (Acá SÍ se usó NuGet, a diferencia de Anthropic, porque la auth del SA firma un JWT RS256 que a mano es un dolor.)
- Endpoints en `ConversacionController` (ambos gateados a admin/secretario con `Forbid`): `POST /api/Conversacion/{id}/extraer-cita` y `POST /api/Conversacion/{id}/crear-cita`.
- Registros en `Program.cs`: `AddHttpClient("Anthropic")`, `AddScoped<IGoogleCalendarService,...>`, `AddScoped<IAgendaIaService,...>`.

**Frontend (panel de cita en `contact-panel`, en tema oscuro):**
- `conversacion.service.ts`: interfaces `MascotaCita`, `CitaExtraidaDto`, `CrearCitaDto`, `CitaCreadaDto`, constante `SLOTS_AGENDA` (lista fija de 10 slots, debe coincidir con `AgendaSlots` del backend), métodos `extraerCita(id)` y `crearCita(id, dto)`.
- `contact-panel.component.ts/html/scss`: botón "🗓 Agendar cita (IA)" → loading → panel con: datos editables, checks de seguro/estacionamiento, **lista de mascotas** (con badge "se creará"/"ya registrada" + aviso de nombre parecido por Levenshtein), selector **Móvil 1/2**, **día** (date picker), **horario** (dropdown), y botón "✓ Crear cita en el calendario".
  - **Título y descripción se generan EN VIVO** (`computed`) desde los campos editables + el slot (no son cajas editables). "Pacientes" se auto-genera desde la lista de mascotas (ej. "1 gato (roberto), 2 perros (marcelo, luna)").
  - Badges "se guardará en el perfil" junto a nombre/dirección/referencias/correo cuando el campo del contacto está vacío y la cita tiene valor.

**Decisiones tomadas (NO re-discutir):**
- Móvil 1/2 = el AUTO que usa el veterinario (rotativo). Lo elige el secretario; es interno (el cliente no lo ve). Define el posicionamiento.
- El cliente ABANDONA los placeholders manuales del calendario; la automatización crea eventos NUEVOS en la posición codificada. Un solo calendario madre; el secretario reparte a cada vet manualmente (a nosotros nos importa solo el madre).
- El slot "1-2 (no siempre)" es decisión del secretario (caso de borde, probablemente manual — no está en el algoritmo).
- Mascotas: la lista estructurada es la fuente de verdad. No se parsea el texto libre "Pacientes"; la IA llena la lista, el secretario la revisa. Dedup por nombre EXACTO (red de seguridad); los casos ambiguos ("chico" vs "shiko") los decide el humano, NO un algoritmo de similitud (solo se avisa).
- Autocompletar perfil: solo rellena campos VACÍOS, nunca sobrescribe.

### 2.B — Tema oscuro del buzón (paleta GramVet)
Panels verde profundo `#0F332E`, gap `#0B2B26`, acento `#6ECFAB`. Convertidos: `.inbox-container .card` (en `_custom.scss`), `conversations`, `chat-window`, `contact-panel`, `inbox.component`. Los módulos de gestión (Usuarios/Tags/Macros) quedan CLAROS a propósito.
- Header topbar: **ocultos** (comentados) los íconos de notificaciones/filtros/mensajería y el toggle de tema. `colorMode` fijado en `'light'` en `app.component.ts` (para que gestión sea legible; la oscuridad viene del shell + buzón).

### 2.C — Colores del chat (últimos ajustes)
- Fondo del área de conversación: **gris oscuro `#2B2B2B`**.
- Burbujas mías (outbound): **verde `#608439`** (sólido), texto blanco.
- Burbujas del contacto (inbound): **gris claro `#EAEAEA`**, texto `#1f2937`.

### 2.D — Menú lateral y hovers en morado
- Opción activa del menú lateral: **morado `#55456F`** (en `_custom.scss` `.sidebar .nav-link.active`).
- Hover de filas en las listas de **Usuarios, Etiquetas y Respuestas rápidas**: **morado sólido `#55456F` con texto blanco** (idéntico en los tres). En Respuestas, el chip `/comando` se pone blanco en hover. En Etiquetas el chip mantiene su color propio (identifica la etiqueta).

### 2.E — Estandarización visual de módulos (Grupo A) — HECHO
Usuarios/Tags/Macros ahora idénticos: headers iguales (padding 18/24, título 15px color `#1a2e25`, subtítulo 12px `#6c757d`, ícono 36px `#1A4A3C` stroke `#6ECFAB`, badge `#E1F5EE`/`#085041` DM Mono, línea divisoria inferior `1px rgba(0,0,0,0.08)`); inputs y lista de Usuarios en blanco `rgba(255,255,255,0.85)` con hover; botones de crear de tags/macros reemplazados por `.btn-create` (ícono + verde claro, texto blanco, bg `#1A4A3C`) igual a "Crear usuario".

### 2.F — Fotos de perfil de usuarios (gestión)
- Nuevo endpoint `POST /api/Usuario/{id}/foto` (sube a R2, borra la anterior — reusa `ActualizarFoto`). El de "mi foto" del header sigue aparte.
- `usuario.service.ts`: método `subirFotoUsuario(id, file)`.
- `users.component`: **selector de imagen opcional al crear** (avatar preview + botón); **avatar editable en la lista** (click abre file input, overlay de cámara en hover, "…" mientras sube).

### 2.G — Scroll en las listas de gestión
Usuarios/Tags/Macros: el card-body es flex-column de altura acotada (header + form + buscador fijos) y la lista (`.table-container` con `flex:1; overflow-y:auto; min-height:0`) scrollea internamente. En tags/macros se agregó clase `.component-body` al `c-card-body` y `.tag-list`/`.macro-list` como flex.

### 2.H — Columna de filtros del buzón ELIMINADA
Se quitó el componente `app-inbox-sidebar` (la columna "Buzón / Todos / Míos / Sin asignar / Nuevo cliente") de `inbox.component.html`/`.ts`. El chat gana ese espacio. **OJO:** NO se tocó el ítem "Buzón (CRM)" del menú lateral (`_nav.ts`) — ese quedó como está (fue un malentendido que se revirtió).

### 2.I — Empty-state del chat
Cuando NO hay conversación seleccionada: `.scroll-container.is-empty` muestra el **wallpaper** `assets/images/vet_chat_wallpaper_wide.png` (cover) con el mensaje "Seleccione una conversación" centrado (chip translúcido). Se **ocultan** el `c-card-header` del chat y el `c-card-footer` (composer) cuando no hay conversación. La imagen fuente está en `Assets/vet_chat_wallpaper_wide.png` y se copió a `Frontend/src/assets/images/`.

### 2.J — SESIÓN 2026-07-09 (parte 2): rediseño gris, captcha y sesión móvil

**Rediseño "gris oscuro + verde" del buzón.** Los 3 paneles pasaron de verde profundo a gris `#2B2B2B`; los verdes GramVet quedan como ACENTO (inputs, avatares, chips, burbujas propias). Regla nueva en `_custom.scss`: `.inbox-container app-conversations/app-chat-window/app-contact-panel .card { background: #2B2B2B }` — hay que hacerlo ahí porque la regla global `.inbox-container .card` usa `!important` y una regla del componente empata en especificidad. Headers/footer de los paneles en `#232323`. Burbujas: propias `#235347`, del contacto `#3C3C3C` (texto `#E7F2EE`). Botón Enviar con texto blanco; adjuntar/ubicación/emojis con contorno e ícono verde `#6ECFAB`. Padding izquierdo del `.inbox-row` (faltaba, estaba en `0`). Se quitó el filtro oscuro (linear-gradient) de la foto del login.

**Separadores de fecha en el chat (estilo WhatsApp).** `esInicioDeDia(i)` + `etiquetaDia(fecha)` en `chat-window.component.ts`: "Hoy" / "Ayer" / nombre del día (2 a 6 días atrás) / fecha (≥7 días). Se normaliza a medianoche local antes de comparar. Usa `toLocaleDateString('es-CL')` y NO el `DatePipe` porque **el proyecto no registra el locale `es`** (el pipe devolvería el día en inglés). El chip es `position: sticky`.

**Foto del veterinario en el chip de asignado** (lista de conversaciones), con las iniciales como fallback. Mapa `id → fotoUrl` desde `getVeterinarios()`. **OJO:** esa carga estaba dentro de `if (esStaff)` (solo servía para el filtro); se sacó del condicional porque el chip lo ve cualquier rol.

**Header:** burbuja morada `#55456F` con `Nombre Apellido / Rol` a la izquierda del avatar (rol inicial del JWT, refinado con `getMe()`). Menú del perfil en gris `#2B2B2B`, cabecera "Mi cuenta" centrada + divisor. Botón "Agendar cita (IA)" en morado `#55456F`.
- ⚠️ **`default-header.component.scss` NO está enlazado** (el `@Component` no declara `styleUrls`) — es código muerto. Los estilos del header viven en `_custom.scss`.
- ⚠️ En `_custom.scss` hay `.header * { color: ... !important }`: pinta de blanco TODO lo que esté dentro del header. Cualquier cosa con fondo claro ahí adentro necesita `!important` para recuperar su color. Candidato a acotar más adelante.
- `cilImage` **no estaba en `icon-subset.ts`**, por eso el ítem "Cambiar foto de perfil" salía sin ícono. Se agregó a las 3 listas del archivo (import, mapa y enum).

**Loader con mensaje.** `LoaderService.show(mensaje?)` acepta texto opcional bajo el logo; se limpia recién cuando el contador llega a 0. El botón "Agendar cita (IA)" lo levanta a mano con **"Recopilando información (IA)"** porque el `loading.interceptor` excluye `/api/Conversacion` del loader global. El resto de los loaders siguen sin texto.

**CAPTCHA — Cloudflare Turnstile en el login (FUNCIONANDO).**
- Backend: `Service/TurnstileService/` (`ITurnstileService`/`TurnstileService`) verifica el token contra `https://challenges.cloudflare.com/turnstile/v0/siteverify` por **HttpClient** ("Turnstile", registrado en `Program.cs`), sin NuGet. `LoginRequestDto` suma `CaptchaToken`. `AuthController.Login` valida el captcha **ANTES** de mirar credenciales (si no, un bot seguiría probando contraseñas) → `400` si falla, contra el `401` de credenciales.
- Criterios: si `Enabled=false` o falta `SecretKey` → **deja pasar** (config incompleta no debe dejar sin CRM a la clínica; se loguea warning). Si Cloudflare **falla o se cae** → **rechaza** (es el escenario que un atacante provocaría).
- Frontend: script en `index.html` con `?render=explicit`; `LoginComponent` monta el widget (tema oscuro) y **resetea el widget en cada error** — el token de Turnstile es de **un solo uso**. Botón deshabilitado hasta resolver el captcha.
- Se quitaron "Recuérdame" (no hacía nada) y "¿Olvidaste tu contraseña?" del login.

**Environments centralizados (`src/environments/`).** `environment.ts` (dev) y `environment.production.ts` + `fileReplacements` en `angular.json`. Una sola constante `backendUrl` de la que derivan `apiUrl` y `hubUrl` (el hub de SignalR cuelga de `/hubs/chat`, no de `/api`). Migrados los 8 services + el site key de Turnstile. **Ya no hay ninguna URL hardcodeada.** `environment.production.ts` todavía apunta a `localhost:7101` (TODO: dominio real).

**Sesión persistente para los veterinarios (usan el CRM desde el celular).**
- El token pasó de `sessionStorage` a **`localStorage`**: en el celular el SO descarta la pestaña cuando el vet pasa un rato en otra app (WhatsApp/Waze) y volvía deslogueado aunque el token siguiera vigente.
- Como consecuencia, un token VENCIDO ahora sobrevive: `AuthService.isLoggedIn()` valida el claim `exp` y el `authGuard` limpia el token antes de mandar al login.
- **`unauthorized.interceptor.ts`** (nuevo, registrado último en `app.config.ts`): ante un `401` hace logout + redirige. **Ignora el 401 de `/Auth/login`**, que significa "credenciales incorrectas" y no sesión vencida.
- `Jwt:ExpireMinutes` de 480 → **600** (10 horas) en `appsettings.json`.
- **Deuda pendiente:** refresh token (lo correcto de verdad). Hoy a las 10 h el vet se reloguea.

---

## 3. DATOS NO RECONSTRUIBLES (config, credenciales, restricciones)

### Anthropic (Claude API) — CONFIGURADO Y FUNCIONANDO
- `appsettings.json` sección `Anthropic`: `{ "ApiKey": "sk-ant-...", "Model": "claude-haiku-4-5" }`.
- ⚠️ **La API key se pegó completa en el chat de esta sesión.** Se recomendó **rotarla** (borrar en console.anthropic.com → crear otra → pegar la nueva en appsettings). No sabemos si el usuario ya la rotó — preguntar/recordar.
- Costo ~9 CLP por extracción (Haiku 4.5).

### Google Calendar — CONFIGURADO Y FUNCIONANDO (calendario DE PRUEBA)
- `appsettings.json` sección `GoogleCalendar`:
  - `ServiceAccountKeyPath`: `E:\Workspace\GRAMVET\GramVet CRM\Backend\GramVetCRM.Api\gramvet-crm-calendar-test-b49a9cfabeab.json`
  - `CalendarId`: `gramvetmovil3@gmail.com` (calendario **de prueba**, vacío, cuenta nueva que dio el cliente)
  - `TimeZone`: `America/Santiago`
- **Service Account:** proyecto Google Cloud "GramVet CRM Calendar Test", email `gramvet-agenda@gramvet-crm-calendar-test.iam.gserviceaccount.com`. El JSON de clave está en `Backend/GramVetCRM.Api/gramvet-crm-calendar-test-b49a9cfabeab.json` (**ya en `.gitignore`** — reglas: `gramvet-crm-calendar-test-*.json`, `*-service-account*.json`, `google-sa.json`, `google-service-account*.json`). El calendario de prueba está compartido con el email del SA con permiso "Realizar cambios en los eventos".
- **Para pasar al calendario REAL de la veterinaria** (cuando terminen las pruebas): compartir el calendario real con el email del SA (mismo permiso) y cambiar `CalendarId` en appsettings. Nada de código.

### Cloudflare Turnstile (captcha del login) — CONFIGURADO Y FUNCIONANDO
- `appsettings.json` sección `Turnstile`: `{ "Enabled": true, "SecretKey": "..." }` (el secret lo puso Sebastian; NO commitear).
- **Site key (pública):** `0x4AAAAAADyzWZlofvQpjo8j` — vive en `src/environments/environment*.ts`.
- Widget creado en la cuenta Cloudflare del cliente (la misma del R2), modo **Managed**, hostname **`localhost`**.
- Turnstile valida el hostname donde se **renderiza el widget** (el frontend, `localhost:4200`), NO el del backend. Por eso el dominio de ngrok (que tunelea el backend) no hace falta en la lista.
- **Para producción:** agregar el dominio real a los hostnames del widget en el dashboard, o el captcha rechaza el login. Si alguna vez se sirve el FRONTEND por ngrok, conviene poner `Enabled: false` (la URL free cambia en cada reinicio).

### Restricciones y datos que siguen del traspaso anterior (vigentes)
- WhatsApp: número de PRUEBA **+56 9 2172 4181**, PhoneNumberId `1158710177318339`. **NUNCA tocar el WhatsApp Business real del cliente.** Verify token `gramvet_gonzalord`.
- Meta: app **VetCRM**, Page ID `101078882762015`, verify token `gramvet_meta_verify`. Instagram bloqueado por App Review.
- R2 (media): bucket `gramvetcrm-media`, URL pública `https://pub-fa94e83d8318492d87b9d41e72b93de0.r2.dev`.
- BD: `GramVetCRM_DEV` en `localhost\SQLEXPRESS`, `Trusted_Connection=True;TrustServerCertificate=True`. sqlcmd en `C:\Program Files\Microsoft SQL Server\Client SDK\ODBC\170\Tools\Binn\sqlcmd`. **Todas las migraciones al día** (usuario confirmó: reaccion, usuario_foto, mensaje_estado_entrega aplicadas). La feature de IA NO agregó columnas ni tablas.
- URLs dev: Backend `https://localhost:7101`, Frontend `http://localhost:4200` (`npx ng serve`).
- ngrok: carpeta `E:\Workspace\Ngrok\ngrok-v3-stable-windows-amd64`. En **PowerShell**: `.\ngrok.exe http https://localhost:7101 --host-header="localhost:7101"` (en cmd, sin el `.\`). URL cambia al reiniciar → actualizar webhooks de Meta.

### Gotcha de build del backend (IMPORTANTE)
Con la API corriendo (proceso `GramVetCRM.Api`) o Visual Studio abierto, `dotnet build` de la solución/Api falla con **MSB3021/MSB3027 (DLL bloqueado)** — NO son errores de código. Para verificar que compila: `dotnet build GramVetCRM.Service/GramVetCRM.Service.csproj -nologo -clp:ErrorsOnly` (0 errores = OK). Filtrar errores reales con `grep "error CS"`. Para reconstruir de verdad, el usuario para la app desde VS. **Al agregar el NuGet de Google hubo que parar la app y reconstruir en VS.**

### Stack (recordatorio)
Frontend Angular 21 + CoreUI + standalone + Signals + SCSS. Backend .NET 8, C#, EF Core, SQL Server, capas Api→Service→Repository→Model. SignalR (`ChatHub`), serializa a camelCase. Roles: 1=Admin, 2=Secretario, 3=Veterinario. Fuentes DM Sans/DM Mono globales.

---

## 4. ESTADO DE GIT

- **Último commit pusheado:** `13435d8 SVN: fotos de usuarios, ajustes visuales y pantalla de bienvenida del chat` (la tanda visual anterior YA está pusheada).
- **SIN COMMITEAR — sesión 2026-07-09 parte 2** (el usuario commitea/pushea él mismo):
  - **Nuevos:** `Backend/.../GramVetCRM.Service/TurnstileService/` (interface + service), `Frontend/src/environments/` (dev + production), `Frontend/src/app/core/interceptors/unauthorized.interceptor.ts`
  - **Backend:** `AuthController.cs`, `Program.cs`, `LoginRequestDto.cs`, `appsettings.json` (**NO subir** — tiene secretos: `Turnstile:SecretKey`, `ExpireMinutes: 600`)
  - **Frontend config:** `angular.json` (fileReplacements), `app.config.ts`, `index.html` (script Turnstile), `icon-subset.ts` (`cilImage`)
  - **Frontend auth/sesión:** `auth.service.ts`, `auth.guard.ts`, `login.component.ts/html/scss`
  - **Frontend services (environments):** `usuario`, `etiqueta`, `contacto`, `conversacion`, `respuesta-rapida`, `signalr`
  - **Frontend UI:** `_custom.scss`, `inbox.component.scss`, `chat-window.component.ts/html/scss`, `conversations.component.ts/html/scss`, `contact-panel.component.ts/scss`, `loader.service.ts`, `loader.component.ts`, `default-header.component.ts/html`

---

## 5. PRÓXIMOS PASOS (en orden)

1. **Commitear/pushear la tanda de la sección 4** (usuario). Recordar NO subir `appsettings.json`.
2. **Rotar la API key de Anthropic** (quedó expuesta en un chat) si no se hizo.
3. **Cuando terminen las pruebas de agenda:** pasar del Google Calendar de prueba al **real** (compartir el real con el SA + cambiar `CalendarId`).
4. **Opcionales de la feature IA a confirmar con el cliente** (no urgentes): color del evento por móvil/vet, mostrar el veterinario asignado en el evento, recordatorios/notificaciones del evento.
5. **Integración Instagram** — backend listo; bloqueado por Meta App Review (se cierra en el hito Go Live, no es código).
6. **"Visto" en Messenger** (webhooks `message_deliveries`/`message_reads`, por watermark) y **reacciones Messenger/IG** — nice-to-have, junto con la config de webhooks de IG.
7. **Deuda técnica antes de prod:**
   - **Refresh token** (hoy: `localStorage` + 10 h + interceptor de 401; el vet se reloguea 1 vez al día).
   - Validación de rol en el BACKEND para Usuarios/Etiquetas/Respuestas (hoy solo frontend + guards; el usuario dijo "dejalo así por ahora").
   - Mover secretos de `appsettings.json` fuera de git.
   - Acotar `.header * { !important }` en `_custom.scss` (rompe cualquier fondo claro dentro del header).
   - Borrar o enlazar `default-header.component.scss` (hoy es código muerto).
   - Limpiar rutas demo del template.
8. **Para producción:** Meta App Review + Live (WhatsApp/Messenger/IG con clientes reales); hosting + HTTPS real (geolocalización necesita HTTPS); apuntar `environment.production.ts` al dominio real y **agregar ese dominio a los hostnames del widget de Turnstile**.

### Ideas visuales sugeridas y NO aplicadas (esperan decisión de Sebastian)
- Hover del header del chat: usa `rgba(50,31,219,0.15)` (azul-violeta ajeno a la paleta). Se propuso pasarlo al morado `#55456F`.
- Ítems de la lista de conversaciones: siguen con degradado verde en seleccionado/hover sobre el fondo gris. Sebastian confirmó que le gusta la combinación verde+gris.
- `.cita-form` (panel de "Agendar cita (IA)") sigue en verde `#11392F` sobre el gris. Se ofreció pasarlo a gris con borde verde.

**Fuera de alcance (NO desarrollar):** Dashboard (sacado explícitamente por Sebastian).

---

## 6. CÓMO RETOMAR (primeros pasos)

1. Leé la memoria (se carga sola) + este doc + `Docs/TRASPASO_2026-06-28.md` (base) + `calendar_logic.md` (lógica del calendario, crítica).
2. `git status` para ver la tanda visual sin commitear.
3. La feature IA + Calendar **ya funciona con datos reales** — no hay que rehacerla. Si hay que retocarla, el flujo está en `AgendaIaService` (backend) y `contact-panel.component` (frontend, panel de cita).
4. Respetá el workflow (sección 1): commits y migraciones los hace el usuario; vos solo das el texto/script.
